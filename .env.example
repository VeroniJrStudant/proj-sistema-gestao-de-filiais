# Copie para `.env` e ajuste se necessário.
# Este ficheiro mantém o SQLite deste projeto separado do `proj-sistema-creche` (que usa `prisma/dev.db` por convenção).
DATABASE_URL="file:./prisma/gestao-filiais-dev.db"

# Opcional: senha aplicada aos utilizadores criados pelo seed (`npm run db:seed`).
# SEED_DEFAULT_PASSWORD="Teste@123"
